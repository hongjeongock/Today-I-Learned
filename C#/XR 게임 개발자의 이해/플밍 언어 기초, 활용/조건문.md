## if문
```csharp
if ()//여기 내용이 참일 경우
{
    //여기 내용 실행
}
```

🔽 if 문으로 조건 처리
```csharp
static void Main()
{
	int score = 60;
	
	if(score >= 60)//score가 60 이상이면
	{
		Console.WriteLine("합격"); // 합격 출력
	}
}
```

🔽if문과 연산자
```csharp
static void Main()
{
	int x = 10;
	
	if(x == 10)
	{
		Console.WriteLine($"x는 {x}입니다.);
	}
	if(x != 20)
	{
		Console.WriteLine($"x는 20이 아닙니다.");
	}
}
```

🔽한 번에 여러 조건 처리
```csharp

```
## else if, else
```csharp
if ()//여기 내용이 참일 경우
{
    //여기 내용 실행
}
else if ()//위에 내용이 거짓이면?
{
    //여기 내용 실행
}

```
콘솔의 글씨색을 초록색으로 바꿔라
건강하다는 멘트를 출력하라
텍스트 색을 원복시켜라

```csharp
int skillCoolTime = 10;

if(skillCooTime <= 0)//5 이상 맞아서 실행
{
    Console.WriteLine("스킬 사용")
}
else if(skillCoolTime >= 5) //3이상인 조건은 맞지만 실행은 안됨
{
    Console.WriteLine("진동")
}
else if(skillCoolTime >= 3)
{
    Console.WriteLine("약하게 진동")
}
```
## Tryparse
```csharp
string input = Console ReadLine();
bool isConverted = int. TryParse(input, out int inputValue);

if (isConverted)

Console.WriteLine($"숫자 입력 : {inputValue}");

else

Console.WriteLine("개발자 괴롭히지 말고 다시 입력하세요");
```
## Switch문
일치하는 분기점에 따른 행동

switch문 -> 일치하는 분기점에 따른 행동

if문은 참거짓으로 실행여부 판단

switch(판단하고자하는수)

case 첫째로여기와같다면:

할일하기

break; : 케이스의 끝

case 둘째로이거와같다면

해당하는 역할

break; 케이스의 끝

default: 위 어느것도 아니라면. 마치 else문과 비슷함



```csharp
string input = Console.ReadLine();
int inputValue = int.Parse(input);

if(inputValue == 1)

Console.WriteLine("마을로 이동");

else if (inputValue == 2)

Console.WriteLine("상점으로 이동");

else if (inputValue == 3)

Console.WriteLine("던전으로 이동");

string input = Console ReadLine ();
int inputValue = int.Parse(input);
```
Switch
```csharp
switch (inputValue)
{
case 1:
    Console.WriteLine("마을로 이동"); I
    break;
case 2:
    Console.WriteLine("상점으로 이동");
    break;
case 3:
    Console.WriteLine("던전으로 이동");
    break;
default:
    Console.WriteLine("아무것도 하지 않음");
    break;
}
```
# 반복문
## while
1. 조건식
    - false라면 바로 종료
    - true라면 로직 수행

```csharp
while(여기 내용이 참이면) 
{
    여기 내용 반복
}
```
```csharp
while(true)
{
    Console.Write("반복문 루프 도는중");
}
```
이런식으로 조건문을 작성하면 무한으로 반복이 된다.

특정 횟수만 돌리고 싶다면
```csharp
int count = 0 
int toLoop = 10;

while(count < toLoop)
{
    count++;
    Console.Write($"반복문 {count}회 루프 수행중");
}
```
짝수만 출력 하고싶을때
```csharp
int count = 0 
int toLoop = 10;

while(count < toLoop)
{
    count++;
    
    if (count % 2 == 0)
    Console.Write($"반복문 {count}회 루프 수행중");
}
```

```csharp
string input;
int inputValue;
bool isConverted

while ()
{
    COnsole.Write("제발 정수좀 입력 해주세요: ");
    input = Console.ReadLine();
    isConverted = int.TryParse(input, out inputValue);

    if (isConverted)
    {
        Console.WriteLine("정수가 입력되었습니다.");
    }
}
    Console.WriteLine("프로그램 끝");
```
do while
1. 로직수행
2. 조건식
    - false라면 바로 종료
    - true라면 반복
```csharp
bool isLoop =true

do
{
    Console.WriteLine("do-while 반복");
}
```
## break, Continue
break : 루프가 즉시 종료되는 키워드
continue : 

```csharp
string input;
int inputValue;
bool isConverted = false;

while (true)
{
    COnsole.Write("제발 정수좀 입력 해주세요: ");
    input = Console.ReadLine();
    isConverted = int.TryParse(input, out inputValue);

    if(isConverted == false)
    {
        //숫자 0 입력 받으면 종료되도록 처리하고 싶을때
        if (inputValue == 0)
        {
            Console.WriteLine("0 입력, 반복문 종료");
            break;
        }
        Continue;
    }

    if (isConverted)
    {
        Console.WriteLine("정수가 입력되었습니다.");
        break;
    }
}
    Console.WriteLine("프로그램 끝");
```
# for문
```csharp
for(초기식;조건식;증감식)
{
    반복할내용;
}
```
1. 초기식
2. 조건식
    - 내부 로직 수행
3. 증감식
4. 2번부터 반복

초기식: 반복을 몇부터 시작할것인지?

```csharp
int counter;

for(int i = 0; i < 10; i++)
{
    Console.WriteLine($"반복{i}회 실행");
}
```
<img width="758" height="520" alt="image" src="https://github.com/user-attachments/assets/18a5f0b1-2a30-4f0a-877e-ae544051b5e5" />

유저에게 숫자를 두개 입력받아서, 그 사이의 수를 모두 더한 합을 구할때
4,9
```csharp
 int firstValue = int.Parse(Console.ReadLine());
 int secondValue = int.Parse(Console.ReadLine());

 int sum = 0;
 for (int i = firstValue; i <= secondValue; i++)
 {
     sum += i;
 }

 Console.WriteLine(sum);
```

<img width="286" height="109" alt="image-1" src="https://github.com/user-attachments/assets/a5cf3533-f6ec-44a1-b47a-98235ccb8436" />

```csharp
 int floor = 15;

 for (int i = 1; i <= floor; i++)
 {
     for (int j = 1; j <= floor - i; j++)
     {
         Console.Write(" ");
     }

     for (int j = 1; j <= (2 * i - 1); j++)
     {
         Console.Write("*");
     }

     Console.WriteLine();
 }
```
출력 하면

<img width="382" height="335" alt="image-2" src="https://github.com/user-attachments/assets/859ed630-6030-40b4-b8bf-9e62c0ae1273" />

어케하셨지..




