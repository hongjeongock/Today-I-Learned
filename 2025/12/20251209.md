# Hello World
프로그램 코드는 위에서 아래로 절차대로 실행이 된다.

```csharp
Console.WriteLine("");// 한 줄단위로 쓰는것
Console.Write(""); //글자를 쓰기만 하는것
```

공식 C# 마이크로소프트 페이지에 레이아웃 규칙과 주석달기 권고 사항. 필수사항은 아니나, 참고하면 좋음
<style type="text/css">
.tg  {border-collapse:collapse;border-spacing:0;}
.tg td{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  overflow:hidden;padding:10px 5px;word-break:normal;}
.tg th{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  font-weight:normal;overflow:hidden;padding:10px 5px;word-break:normal;}
.tg .tg-7btt{border-color:inherit;font-weight:bold;text-align:center;vertical-align:top}
.tg .tg-0pky{border-color:inherit;text-align:left;vertical-align:top}
</style>
<table class="tg"><thead>
  <tr>
    <th class="tg-7btt">레이아웃 규칙</th>
  </tr></thead>
<tbody>
  <tr>
    <td class="tg-0pky">1. 문은 한줄에 하나씩만 작성</td>
  </tr>
  <tr>
    <td class="tg-0pky">2. 선언은 한 줄에 하나씩만 작성</td>
  </tr>
  <tr>
    <td class="tg-0pky">3. 메서드 정의, 속성 정의 간에는 빈줄을 하나 이상 추가</td>
  </tr>
  <tr>
    <td class="tg-0pky">4. 한 줄에 여러 식이 논리 결합 되어있을시, 괄호로 명확하게 구분</td>
  </tr>
</tbody>
</table>

### 줄 넘기기
```csharp
Console.WriteLine("내용");
Console.WriteLine();
Console.WriteLine();
Console.WriteLine();
Console.WriteLine("내용2");
```
### 주석
컴퓨터가 읽는 코드가 아닌 프로그래머가 읽기 위한 용도. 컴퓨터는 읽지 못한다.
```csharp
Console.WriteLine(); //슬래쉬 두개를 사용하여 주석 입력

/*Console.WriteLine(); 
여러줄에
대한
주석
작성
가능
*/
```
<style type="text/css">
.tg  {border-collapse:collapse;border-spacing:0;}
.tg td{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  overflow:hidden;padding:10px 5px;word-break:normal;}
.tg th{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  font-weight:normal;overflow:hidden;padding:10px 5px;word-break:normal;}
.tg .tg-7btt{border-color:inherit;font-weight:bold;text-align:center;vertical-align:top}
.tg .tg-0pky{border-color:inherit;text-align:left;vertical-align:top}
</style>
<table class="tg"><thead>
  <tr>
    <th class="tg-7btt">주석달기 규칙</th>
  </tr></thead>
<tbody>
  <tr>
    <td class="tg-0pky">1. 주석은 코드의 끝이 아닌 별도의 줄에 배치</td>
  </tr>
  <tr>
    <td class="tg-0pky">2. 영어 주석일 경우, 주석의 첫 글자는 대문자로 시작</td>
  </tr>
  <tr>
    <td class="tg-0pky">3. 주석의 끝에는 마침표를 찍는다.</td>
  </tr>
  <tr>
    <td class="tg-0pky">4. 코드를 위한 설명 주석일 경우, 별포 주석은 쓰지 않는다.</td>
  </tr>
</tbody>
</table>

### 들여쓰기

### Escape sequnce, 확장열
문자열 안에서 쓸 수 있는 일종의 명령어로 이해하면 편하다.
\a : 벨(경고)
\n : 줄 바꿈
\t : 가로 탭
 
 줄바꿈 예시
```csharp
Console.WriteLine("내용\n");
Console.WriteLine("내용\n");
Console.WriteLine("내용");
```
가로탭 예시

```csharp
Console.WriteLine("내용\t추가텍스트") //"내용	추가텍스트" 로 한줄에 출력 됨
```

### using 과 namespace

namespace : 폴더 안에다가 

```csharp 
namespace playerExtension
{

}
```

### 자리표시자

출력기능의 심화 {0}, 출력하고자 하는 내용

```csharp
Console.WriteLine("내이름 홍길동 입니다.");
```


### 변수
컴퓨터에게 데이터를 저장 시키는것

```csharp
Console.WriteLine(46);
```
#### 변수를 사용하는데 필요한 세가지 요소
1. 저장하고자 하는 대상의 형태
2. 저장소의 이름
3. 실제 값

변수 '선언' 변수 선언 정수형a; 값을 할당하다

```csharp
int value;	//변수의 선언

value = 55	

Console.WriteLine(value);

// DataType		Name		Value
	int 		value 	=	 55;	//변수를 선언하면서 초기화
```
정수, 실수 등등 자료의 형태: 자료형

```csharp
int firstNum = 5 ;

firstNum = 7; // 기존에 저장했던 5는 무시되고 7이 출력된다.
```

#### 변수명 규칙

### 상수
선언후 변하지 않는 수

```csharp
const int persnalID = 2;
```

### 자료형
일반적인 정수 int형 : 정수형
```csharp
int intvalue = 15;
```
문자 하나 담는 char형 : 문자 하나 단위
```csharp
char charValue = 'a';
```
문자열 string 
```csharp
stringValue = "문자열 희희"'
```

실수형 float
```csharp
float floatValue = 4.7f;
```

참 거짓 bool : true / false
```csharp
boolValue = true;
```
### 자리표시자, 문자열 보간
예시] 플레이어의 데이터를 출력해야 한다.
```csharp
string name = "kim";

int higScore = 87;

bool isOnline = false;

//자리표시자
Consoel.WriteLine("플레이어 이름 :{0} \최대 점수 : {1}\접속 유무 : {2}, name, higScore, isOnline);

//문자열 보간
Console.WriteLine($"플레이어 이름: {name}\n최대 점수: {higScore} \n접속 유무: {isOnline}" );
```
출력 화면

![alt text](2025/12/20251217/image.png)

### 연산자
프로그램은 무수한 사칙연산의 집합체이고

#### region
접고자 하는 내용의 시작점에 #region 끝나는 지점에 #endregion

region옆에 이름 지정 가능 (#region 변수)

#### 대입 연산자
왼쪽의 값에 오른쪽 내용을 대입 하겠다 : =

```csharp
Console.WriteLine(5 + 2);
Console.WriteLine(5 - 2);
Console.WriteLine(5 * 2);
Console.WriteLine(5 / 2); //몫만 출력된다.
Console.WriteLine(5 % 2); //나머지만 출력된다.
```
```csharp
Console.WriteLine("22나누기 3의 몫은 {0}", 22 / 3);
Console.WriteLine("22나누기 3의 나머지는 {0}", 22 % 3);

int firstNum = 3;
int secondNum = 9;
Console.WriteLine(firstNum + secondNum);
```
```csharp
//a = b +c;
int result;
result = firstNum * 2 + secondNum * 3 + firstNum;
```
int a =3;

a = a + 1;

a = 3 +1;

a = 4가 들어있음

```csharp
// 공격력에서 플레이어 방어력을 뺀 값에 보정치를 곱한 값이 최종 데미지
int playerDef = 5;
int monsterAtk = 10;
int value = 2;
//괄호를 써서 우선순위를 지정해줘야한다.
Console.WriteLine($"플레이어가 {(monsterAtk - playerDef) * value} 데미지를 입었다.");
```
```csharp
int a = 1;
int b = 2;

a = a + b;
```
를 좀 더 간단하게

```csharp
int a = 1;
int b = 2;

a += b;
```
= 앞에 +를 붙이면 된다.(+, -, /, *. % 다 가능)

증감연산자
```csharp
int count = 0;

Console.WriteLine(count);
```
#### 전위연산, 후위연산
```csharp
int count1 = 0;
int count2= 0;

Console.WriteLine(count1++); //후위연산
Console.WriteLine(++count1); //전위연산
```
평균 구할때..
```csharp
int a = 5;
int b = 6;
int c = 8;

flot average = (a + b + c) / 3;
Console.WriteLine(average);
```
6이 출력 된다.
```csharp
int a = 5;
int b = 6;
int c = 8;

flot z = (a + b + c) / (float)3;
Console.WriteLine(average);
```
6.333333 이 출력 된다. 이를 형변환이라 부른다

### 비교 연산자
#### 같음연산자(Equality Operator)

• A == B : A, B가 같다면 true, 같지 않다면 false를 반환한다.

• A != B : A, B가 같다면 false, 같지 않다면 true를 반환한다
```csharp
// 같음 / 다름
result = a == b;
result = a !=b;

```

#### 비교연산자(Comparison Operator)

• A > BI: A가 B 초과라면 true, 아니라면 false를 반환한다

• A < B : A가 B 미만이라면 true, 아니라면 false를 반환한다

• A >= B : A가 B 이상이라면 true, 아니라면 false를 반환한다

• A <= B : A가 B 이하라면 true, 아니라면 false를 반환한다
```csharp
int a = 5;
int b = 66;

Console.WriteLine(a > b); // false
Console.WriteLine(a > b); // false
Console.WriteLine(a > b); // false
```
```csharp
int a = 5;
int b = 66;

bool result = a > b;

Console.WriteLine(result) ;
```
```csharp
int a = 5;
int b = 66;
bool result;

// 초과 | 미만
result = a > b;
result = a < b;

// 이상 / 이하
result = a >= b;
result = a <= b;

Console.WriteLine(result);
```

#### 논리연산자

두개 이상의 bool타입 데이터에 대한 논리 연산을 수행한다.

크게는 사용 목적에 따라 Bool 논리연산자, 조건부 논리연산자로 나눠볼 수 있다.

다만, 조건부 논리연산자 쪽이 효율적으로 형태로 동작하기 때문에 자주 쓰이는 방식이다.

그럼에도 불구하고 Bool 논리연산자는 이진수 타입을 연산해야 할 때도 사용되므로 알고
있어야 할 개념이다.

연산자는 기본적으로 이진 형태의 데이터, bool 타입에 대해
수행하는 것이 일반적이다. 연산자의 종류는 아래와 같다.

• NOT(!) : 제시된 하나의 조건에 대해 반대로 출력

• AND ( &) : 제시된 데이터들이 모두 true일때만 true 출력

• OR ( | ) : 제시된 데이터들 중 하나 이상이 true일 때true 출력

• XOR (^) : 제시된 데이터들이 서로 같지 않을 때 true 출력

```csharp
bool a = true;
bool b = false;
bool c = true;
bool d = false;

bool result = a | b | c | d;

Console.WriteLine(result);
```
결과 값

![alt text](image-1.png)

```csharp
bool a = true;
bool b = false;
bool c = true;
bool d = false;

bool result = a & b & c & d;

Console.WriteLine(result);
```
결과값

![alt text](image-2.png)

점프
1. 키 눌림
2. 바닥에 붙어있어야 함
3. 캐릭터 살아있음
4. 다른 상태가 아닌 경우

```csharp
bool isKeyDown = true;
bool isGrounded = true;
bool isAlive = true;
bool isAnotherState = false;

bool canJump = isKeyDown && isGrounded && isAlive && ! isAnotherState;
```
##### 조건부 논리 연산자

마찬가지로 bool 타입에 대한 논리연산을 수행한다.

• AND ( && ) : 주어진데이터들이 모두 true일 때만 true출력

• OR ( || ) : 주어진 데이터들 중 하나 이상 true라면 true출력

하지만 위의 Bool 논리연산자와의 차이점은, Bool 논리연산자는 실제로 주어진 모든 데이터에 대한 논리연산을 수행하지만, 조건부 논리 연산은 이후의 논리연산을 수행할 필요가 없다면 수행하지 않는다.

### RedaLine 입력
컴퓨터가 사용자의 입력을 받고 출력해주는 기능
```csharp
Console.Write("입력 : ");
string input = Console.ReadLine();

Console.WriteLine("유저 입력 완료");

Console.WriteLine(input);
```
![alt text](image-4.png)

### parse
```csharp
Console.Write("숫자 입력 : ");

string input = Console.ReadLine();

Console.WriteLine("유저 입력 완료");

int value = int.Parse(input);

Console.WriteLine($"입력받은 수의 제곱 : {value * value}");

```
